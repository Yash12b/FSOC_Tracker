# Situation model

The deterministic `SituationClassifier` remains the fallback and safety
reference. The learned classifier is trained from observable runtime features
with expert/simulator labels used only as targets. Hidden disturbance state and
ground-truth coordinates are not runtime inputs.

Current generated artifacts are experimental; the recorded test accuracy is
synthetic-dataset evidence only.
